import { Story, Meta } from '@storybook/react'
import ToggleTheme from '.'

export default {
  title: 'ToggleTheme',
  component: ToggleTheme
} as Meta

export const Default: Story = () => <ToggleTheme />
